{
    "private": true,
    "dependencies": {},
    "devDependencies": {
        "@prettier/plugin-php": "^0.16.1",
        "prettier": "^2.2.1"
    },
    "scripts": {
        "test": "echo \"Error: no test specified\" && exit 1"
    }
}
