{
    "private": true,
    "devDependencies": {
        "@prettier/plugin-php": "^0.16.1",
        "prettier": "^2.8.8"
    },
    "scripts": {
        "test": "echo \"Error: no test specified\" && exit 1"
    }
}
